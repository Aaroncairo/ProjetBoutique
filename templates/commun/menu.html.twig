<header class="bg-secondary">
    <nav class="navbar container navbar-expand navbar-light mb-3">
        <ul class="navbar-nav">
            <li class="nav-item">
                <h2>⚽</h2>
            </li>
            <li class="nav-item">
                <a href="{{ path("home_index") }}" class="nav-link text-white">Accueil</a>
            </li>
            <li class="nav-item">
                <a href="{{ path("home_contact") }}" class="nav-link text-white">Contact</a>
            </li>
        </ul>

        <ul class="navbar-nav ms-auto">
            {% if app.user is null %}
            <li class="nav-item">
                <a href="{{ path("app_login")}}" class="nav-link text-white">Connexion</a>
            </li>
            {% else %}
            <li class="nav-item">
            <a href="{{ path("app_logout")}}" class="nav-link  text-white">Deconnexion</a>
            </li>
            {% endif %}
            <li class="nav-item">
                <a href="{{ path("produit_list") }}" class="nav-link text-white">Back Office</a>
            </li>
        </ul>
    </nav>
</header>